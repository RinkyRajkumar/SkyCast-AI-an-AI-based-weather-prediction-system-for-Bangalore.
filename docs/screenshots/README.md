# Screenshot placeholders

Add release screenshots here using these stable names:

- `dashboard-desktop.png` — forecast dashboard at a desktop viewport.
- `dashboard-mobile.png` — stacked dashboard at a mobile viewport.
- `api-docs.png` — FastAPI OpenAPI documentation.

The main README already references this folder so screenshots can be added without changing its structure.
